# Generated by meson-jll v0.0.3
project('HiGHS-x86_64-windows-cxx11', 'c')

cc = meson.get_compiler('c')
fs = import('fs')
include_dirs = []
if fs.is_dir('include')
  include_dirs = [include_directories('include')]
  if fs.is_dir('include/highs')
    include_dirs += [include_directories('include/highs')]
  endif
endif
python3 = find_program('python3', 'python', required: false)


is_msvc = cc.get_id() == 'msvc'
dll_to_lib = meson.current_source_dir() / '..' / 'packagefiles' / 'dll_to_lib.py'




if is_msvc and python3.found() and fs.is_file('bin/libhighs.dll') and not fs.is_file('lib/highs.lib')
  libhighs_import_lib = custom_target(
    'highs-msvc-import-lib',
    input: 'bin/libhighs.dll',
    output: 'highs.lib',
    command: [python3, dll_to_lib, '@INPUT@', '@OUTPUT@', 'X64'],
  )
  libhighs = declare_dependency(link_with: libhighs_import_lib)
else
libhighs = cc.find_library('highs', dirs: meson.current_source_dir() / 'lib', required: false)
if not libhighs.found()
  if not fs.is_file('bin/libhighs.dll')
    error('HiGHS-x86_64-windows-cxx11: no linkable highs in lib/, and bin/libhighs.dll is missing from the extracted tarball')
  endif
  libhighs = declare_dependency(link_args: [meson.current_source_dir() / 'bin/libhighs.dll'])
endif

endif



libblastrampoline_dep = dependency('libblastrampoline_jll')

CompilerSupportLibraries_dep = dependency('CompilerSupportLibraries_jll')

Zlib_dep = dependency('Zlib_jll')

strip_tool = find_program('llvm-strip', 'strip', required: false)
should_strip = get_option('strip') and python3.found() and strip_tool.found()
strip_or_copy = meson.current_source_dir() / '..' / 'packagefiles' / 'strip_or_copy.py'

runtime_dir_name = 'bin'

runtime_dir = meson.current_source_dir() / runtime_dir_name
prune_unused_libs = meson.current_source_dir() / '..' / 'packagefiles' / 'prune_unused_libs.py'
reachable_libs = []
if python3.found()
  prune_result = run_command(
    python3,
    prune_unused_libs,
    runtime_dir,

    'libhighs.dll',

    check: false,
  )
  if prune_result.returncode() == 0
    foreach line : prune_result.stdout().split('\n')
      name = line.strip()
      if name != ''
        reachable_libs += name
      endif
    endforeach
  endif
endif

stripped_separately = should_strip ? ['libhighs.dll',] : []

if reachable_libs.length() > 0
  install_libs = []
  foreach name : reachable_libs
    if should_strip
      custom_target(
        name + '-stripped',
        input: runtime_dir_name / name,
        output: name,
        command: [python3, strip_or_copy, strip_tool.full_path(), '@INPUT@', '@OUTPUT@'],
        install: true,
        install_dir: get_option('libdir'),
      )
    else
      install_libs += runtime_dir_name / name
    endif
  endforeach
  if install_libs.length() > 0
    install_data(install_libs, install_dir: get_option('libdir'))
  endif
else

  install_subdir(
    'bin',
    install_dir: get_option('libdir'),
    strip_directory: true,
    exclude_files: ['highs.exe', ] + stripped_separately,
  )


  if should_strip
    custom_target(
      'highs-stripped',
      input: 'bin/libhighs.dll',
      output: 'libhighs.dll',
      command: [python3, strip_or_copy, strip_tool.full_path(), '@INPUT@', '@OUTPUT@'],
      install: true,
      install_dir: get_option('libdir'),
    )
  endif

endif

highs_dep = declare_dependency(
  version: '1.15.1+0',
  include_directories: include_dirs,
  dependencies: [

    libhighs,


    libblastrampoline_dep,

    CompilerSupportLibraries_dep,

    Zlib_dep,

  ],
)