# Generated by meson-jll v0.0.3
project('Zlib-aarch64-linux-musl', 'c')

cc = meson.get_compiler('c')
fs = import('fs')
include_dirs = []
if fs.is_dir('include')
  include_dirs = [include_directories('include')]
  if fs.is_dir('include/zlib')
    include_dirs += [include_directories('include/zlib')]
  endif
endif
python3 = find_program('python3', 'python', required: false)





libz = cc.find_library('z', dirs: meson.current_source_dir() / 'lib', required: false)
if not libz.found()
  if not fs.is_file('lib/libz.so')
    error('Zlib-aarch64-linux-musl: no linkable z in lib/, and lib/libz.so is missing from the extracted tarball')
  endif
  libz = declare_dependency(link_args: [meson.current_source_dir() / 'lib/libz.so'])
endif




strip_tool = find_program('llvm-strip', 'strip', required: false)
should_strip = get_option('strip') and python3.found() and strip_tool.found()
strip_or_copy = meson.current_source_dir() / '..' / 'packagefiles' / 'strip_or_copy.py'

install_subdir(
  'lib',
  install_dir: get_option('libdir'),
  strip_directory: true,
  exclude_directories: ['cmake', 'pkgconfig', 'gcc'],
  exclude_files: should_strip ? ['libz.so',] : [],
)


if should_strip
  custom_target(
    'z-stripped',
    input: 'lib/libz.so',
    output: 'libz.so',
    command: [python3, strip_or_copy, strip_tool.full_path(), '@INPUT@', '@OUTPUT@'],
    install: true,
    install_dir: get_option('libdir'),
  )
endif


zlib_dep = declare_dependency(
  version: '1.3.2+0',
  include_directories: include_dirs,
  dependencies: [

    libz,


  ],
)